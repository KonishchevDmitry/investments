#!/usr/bin/env bash
#
# Generates regression tests data
#

set -eu

(cd testdata && ./update)

for broker in ib open; do
    ./regression_tests/investments sync "$broker"
done

retest verbose cpus=1 generate regression_tests/rt.rt